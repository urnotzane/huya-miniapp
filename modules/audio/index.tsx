import React, { useState, FC, useEffect } from "react";
import { UI } from '@hyext/hy-ui'

const { View, Text } = UI

export const AudioFc: FC<{}> = () => {
  
  useEffect(() => {

  }, []);
  return (
    <View className="container">
      <Text>7</Text>
    </View>
  )
}